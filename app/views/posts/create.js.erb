<!--id=indexのhtmlを更新する-->
$("#index").html("<%= j( render "posts/index", posts: @posts ) %>");
<!--id=serch_indexのhtmlを更新する-->
$("#search_index").html("<%= j( render "posts/index", posts: @search_posts ) %>");
<!--js-formをclassに指定したfomrを空にする。今回は投稿form-->
$(".js-form")[0].reset();

